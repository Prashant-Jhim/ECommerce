let arr = []
let price = [1000,2000,3000] 
let brand = [apple,samsung] 
let Filter = () =>{
    var arr = main 
    var newarr = []
    for (let i = 0 ; i < arr.length ;i++){
        if (arr.price == 0 && brand.indexOf(arr))
    }
}